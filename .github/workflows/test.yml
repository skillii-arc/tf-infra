name: Starter Workflow Demo

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened

jobs:
  build:
    runs-on: tf-runner
    steps:
      - uses: actions/checkout@v3
      - name: demo workflow job
        run: echo This is a demo start workflow

  deploy:
    runs-on: tf-runner
    needs: build
    steps:
      - name: demo workflow job
        run: echo This is a demo deploy workflow